exports.up = async (sql) => {
  await sql`
    CREATE TABLE IF NOT EXISTS words (
      id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
      lang_1 varchar(100),
			lang_2 varchar(100),
      words_id INT REFERENCES wordlists(id)
    );
	`;
};

exports.down = async (sql) => {
  await sql`
    DROP TABLE IF EXISTS wordLists;
	`;
};
