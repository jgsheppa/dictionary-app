exports.up = async (sql) => {
  await sql`
    CREATE TABLE IF NOT EXISTS words (
      id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
      lang_1 varchar(100) ,
			lang_2 varchar(100),
      list_id integer NOT NULL REFERENCES wordlists(wordlists_id) ON DELETE CASCADE,
      CONSTRAINT one_word_in_list UNIQUE(lang_1,list_id)

    );
	`;
};

exports.down = async (sql) => {
  await sql`
    DROP TABLE IF EXISTS words;
	`;
};
